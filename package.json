{
  "name": "linear-ai-workflow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "cli": "bun src/commands.ts",
    "db:down": "docker compose down",
    "db:generate": "bun --bun drizzle-kit generate",
    "db:migrate": "bun --bun drizzle-kit migrate",
    "db:up": "docker compose up -d --wait",
    "format": "oxfmt --check",
    "format:fix": "oxfmt",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@effect/cli": "0.72.1",
    "@effect/cluster": "0.54.0",
    "@effect/platform": "0.93.5",
    "@effect/platform-bun": "0.85.0",
    "@effect/sql": "0.48.5",
    "@effect/sql-pg": "0.49.7",
    "@effect/workflow": "0.14.0",
    "drizzle-orm": "1.0.0-beta.20",
    "effect": "3.21.2"
  },
  "devDependencies": {
    "@testcontainers/postgresql": "^11.7.2",
    "@types/bun": "latest",
    "drizzle-kit": "1.0.0-beta.20",
    "oxfmt": "^0.42.0",
    "oxlint": "^1.57.0",
    "oxlint-tsgolint": "^7.0.2001",
    "typescript": "~6.0.3"
  }
}
